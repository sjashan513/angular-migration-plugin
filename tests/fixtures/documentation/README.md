Documentation fixtures are intentionally small and generated into temporary Git projects by the documentation contract tests.

`research.template.json` is copied into a run inbox after replacing its run and manifest placeholders. The tests create the eight Markdown files from the same manifest and evidence so hashes, commits, and event IDs remain deterministic for each fixture.
